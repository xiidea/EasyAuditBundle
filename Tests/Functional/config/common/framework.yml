framework:
    secret: test
    test: ~
    session:
        handler_id: null
        cookie_secure: auto
        cookie_samesite: lax
        storage_factory_id: session.storage.factory.native
    router:
        resource: "%kernel.project_dir%/config/common/routing.yml"

services:
    logger: 
        class: Psr\Log\NullLogger
